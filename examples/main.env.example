# ==============================================
# Global Project Configuration
# ==============================================

# Environment mode (development or production)
# - development: More verbose logging, development-specific behaviors
# - production: Minimal logging, optimized for production use
DENO_ENV=development

# Log level (debug, info, warn, error)
# - debug: Most verbose, includes all log messages
# - info: Standard informational messages and warnings/errors
# - warn: Only warnings and errors
# - error: Only error messages
LOG_LEVEL=info

# List of enabled MCP servers (comma-separated)
# If not specified, all servers in the servers/ directory will be enabled
# Example formats:
# - Single server: ENABLED_SERVERS=mcp-atlassian
# - Multiple servers: ENABLED_SERVERS=github-mcp-server, mcp-atlassian, mcp-slack
# - Spaces are trimmed, so spacing doesn't matter
#
# Uncomment and customize the line below to enable specific servers:
# ENABLED_SERVERS=#Optional: mcp-atlassian, mcp-slack

# (Optional) Path to the Cursor MCP configuration file.
# Defaults to a global path like ~/.cursor/mcp.json or %USERPROFILE%/.cursor/mcp.json.
# Set this if you want to use a project-specific mcp.json (e.g., .cursor/mcp.json within your project).
# CURSOR_MCP_CONFIG_PATH=.cursor/mcp.json
